import { ToggleRateLinkDirective } from './toggle-rate-link.directive';

describe('ToggleRateLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleRateLinkDirective();
    expect(directive).toBeTruthy();
  });
});
